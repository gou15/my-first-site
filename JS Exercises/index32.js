let height = 10;
let base = 4;
let perimeterRectangle = (base * 2) + (height * 2);
let areaRectangle = base * height;
console.log(`A rectangle with ${height} height and ${base} base has a perimeter of ${perimeterRectangle} and an area of ${areaRectangle}.`);